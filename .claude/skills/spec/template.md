# PROJ-X: Feature Name

<!-- This file (spec.md) is the stable CONTRACT — it defines WHAT, not HOW.
     Owner: /spec (creates and updates — updates are deltas, IDs never renumbered).
     READ-ONLY during /build. Technical design lives in design.md, verification in review.md.
     Lite spec: Why + ACs + Out of Scope is enough. Full spec (risk work): all sections. -->

## Status: Spec'd

**Created:** YYYY-MM-DD · **Last Updated:** YYYY-MM-DD

## Why

_2–3 sentences: the problem, who it's for._

## Dependencies

- None

## User Stories (full spec)

- As a [user type], I want to [action] so that [goal]

## Acceptance Criteria

**Format:** **AC-N** — Given [a starting state] / When [the user acts] / Then [the observable result] — written in the project's spec language (`AGENTS.md` → Spec Language)

<!-- Stable IDs (AC-1, AC-2, …). review.md verifies per AC-ID. Chain: AC → Test.
     Unresolved ambiguity: [NEEDS CLARIFICATION: exact question] inline, never a silent guess. -->

- [ ] **AC-1** — Given [starting state], when [action], then [result]
- [ ] **AC-2** — Given [starting state], when [action], then [result]

## Out of Scope

- _What this feature explicitly does NOT cover (move dropped behavior here with a one-line reason)_

## Edge Cases (when real)

- **EC-1** — What happens when...?

## Technical Requirements (optional)

- Performance / security / platform expectations, per `AGENTS.md` → Tech Stack

## Open Questions

- [ ] Question 1

## Decision Log

### Product Decisions

| Decision | Rationale | Date |
| -------- | --------- | ---- |
