@AGENTS.md

<!-- AGENTS.md is the canonical, agent-agnostic context file (Codex, Cursor, Copilot, … read it natively).
     This one-line import loads it into Claude Code. Add Claude-only guidance below if ever needed —
     project facts and rules belong in AGENTS.md, not here. -->
